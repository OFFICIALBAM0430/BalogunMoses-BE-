//Create an array called mixedDataTypes that holds at least one boolean, one number, one string, null, undefined,
//and one object. Print the array and its length.

var mixedDataTypes = [
    true,
    12,
    "Moses",
    null,
    undefined,
    name = "Balogun"
];
console.log(mixedDataTypes);
console.log(mixedDataTypes.length);